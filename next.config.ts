// Intentionally removed: this repository hosts the Next app under apps/user-app.
// Having a root-level next.config.ts can confuse build systems like Vercel
// into detecting the root as a Next.js app and failing the build.
